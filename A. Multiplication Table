#include <iostream>
    #include <algorithm>
    #include <cmath>
    #include <string>
    #include <iomanip> 
using namespace std;
int main()
{  long long z,x,s=0;
cin>>z>>x;
if(x<=z){
for(int i=1;i<=x;i++){
   if(x%i==0)s+=1; 
}}
else {
  for(int i=2;i<=z;i++){
   if(x%i==0&&z*i>=x){s+=1;}
}
}
 cout<<s;

    return 0;}
